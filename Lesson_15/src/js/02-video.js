const value = 100;

function add(a, b) {
  return a + b;
}

function sum(a, b) {
  return a * b + value;
}

// export { add, sum };

const a = 15;
const b = 20;

export { add, sum };
export default a;
console.log('hello');
